export function add2(dispatch) {
  // Action mais elaborada

  // fetch("url")
  //   .then((resp) => resp.json())
  //   .then((resp) => dispatch({ type: "userListReady", payload: json }));

  dispatch({ type: "add2" });
}
